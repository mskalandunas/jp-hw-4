'use strict';

// Takes a string of file contents and injects into a target file
function injectStringIntoHTML(string = process.argv[2], targetFile, tag) {
    try {

    }

    catch(e) {
        console.log(e);
    }
}
